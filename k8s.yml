apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-app
  template:
    metadata:
      labels:
        app: weather-app
    spec:
      containers:
      - name: weather-app
        image: rameshkumarverma/nodejs-weather-app:latest 
        ports:
        - containerPort: 3000
        env:
        - name: API_KEY
          value: "a97b930372be92ff6ee04d8ccd5a5a11"
---
apiVersion: v1
kind: Service
metadata:
  name: weather-app-service
spec:
  selector:
    app: weather-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
